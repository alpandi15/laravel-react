import React from 'react'
import Layout from '../../layouts'

const Dashboard = ({
    history
}) => {
    return (
        <Layout history={history}>
            Dashboard
        </Layout>
    )
}

export default Dashboard
